spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/shop?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: atk123!

  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_batch_fetch: 1000
        show_sql: true
        format_sql: true

  devtools:
    livereload:
      enabled: true

  thymeleaf:
    cache: false

  servlet:
    multipart:
      max-file-size: 20MB # 파일 한 개당 최대 사이즈
      max-request-size: 100MB # 요청당 최대 파일 크기

itemImgLocation: /Users/seokjuhong/study/file/item
uploadPath: /Users/seokjuhong/study/file

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace